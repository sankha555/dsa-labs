#include <stdio.h>

extern void push(struct linkedList* head, int ele);
/* push function to push a new element on top of the stack */

extern struct node* pop(struct linkedList* head);
/* pop function to delete and return top element from the stack */
